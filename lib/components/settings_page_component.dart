import 'package:flutter/material.dart';

class SettingsPageComponent extends StatelessWidget {
  const SettingsPageComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
